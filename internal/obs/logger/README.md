# 包说明

## 包作用
- 提供统一日志器构建入口，保证日志格式和级别一致。

## 实现逻辑
1. 根据输入级别创建日志实例。
2. 把日志实例注入其他模块，避免重复初始化。

## 关键接口
```go
func New(level string) (*zap.Logger, error)
```

## 协作关系
- 被启动入口初始化后传递至业务、存储、接口与模型模块。
